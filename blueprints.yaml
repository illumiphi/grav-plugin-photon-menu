name: photon ✴ Menu
version: 0.1.0
description: datatype
author:
  name: phi ARCHITECT
  email: phi@illumiphi.com
homepage: https://github.com/photon-platform/grav-plugin-photon-menu
docs: https://github.com/i-am-phi/grav-plugin-photon-menu/blob/develop/README.md
bugs: https://github.com/i-am-phi/grav-plugin-photon-menu/issues
keywords: datatype, photon
icon: asterisk
license: MIT

dependencies:
    - { name: photon, version: '>=0.1.0' }

form:
  validation: strict
  fields:

    enabled:
      type: toggle
      label: PLUGIN_ADMIN.PLUGIN_STATUS
      highlight: 1
      default: 0
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool

    built_in_css:
      type: toggle
      label: PLUGIN_PHOTON.USE_BUILTIN_CSS
      highlight: 1
      default: 1
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool

    built_in_js:
      type: toggle
      label: PLUGIN_PHOTON.USE_BUILTIN_JS
      highlight: 1
      default: 1
      options:
        1: PLUGIN_ADMIN.ENABLED
        0: PLUGIN_ADMIN.DISABLED
      validate:
        type: bool


    # text_var:
    #   type: text
    #   label: PLUGIN_MENU.TEXT_VARIABLE
    #   help: PLUGIN_MENU.TEXT_VARIABLE_HELP
