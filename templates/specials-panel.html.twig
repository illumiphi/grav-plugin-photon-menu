<section class="panel">

  <header>
    <img class="logo" src="/images/ona-banner.png" alt="logo">
    <div>
      <h1>{{ page.title }}</h1>
      <h2>{{ "now"|date("M d, Y") }}</h2>
    </div>
  </header>

  {% for section in page.collection %}
    {% include "menu-section.html.twig" with { 'section': section } %}
  {% endfor %}


  <footer>
    <!-- <p>* contains raw or undercooked items</p> -->
  </footer>
</section>
